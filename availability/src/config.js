module.exports = {
    redis: {
        host: 'localhost',    // Dirección del servidor Redis
        port: 6379           // Puerto de conexión (por defecto Redis usa el 6379)
    },
    websocket: {
        port: 8080           // Puerto en el que el servidor WebSocket estará escuchando
    }
};
